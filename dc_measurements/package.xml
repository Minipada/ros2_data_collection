<?xml version="1.0"?>
<?xml-model href="http://download.ros.org/schema/package_format3.xsd" schematypens="http://www.w3.org/2001/XMLSchema"?>

<!--
SPDX-FileCopyrightText: 2022-2026 David Bensoussan
SPDX-License-Identifier: MPL-2.0
-->

<package format="3">
  <name>dc_measurements</name>
  <version>0.2.0</version>
  <description>Collect data with measurement plugins</description>
  <maintainer email="d.bensoussan@proton.me">David Bensoussan</maintainer>
  <license>MPL-2.0</license>

  <buildtool_depend>ament_cmake_ros</buildtool_depend>

  <depend>action_msgs</depend>
  <depend>controller_manager_msgs</depend>
  <depend>cv_bridge</depend>
  <depend>dc_common</depend>
  <depend>dc_core</depend>
  <depend>dc_interfaces</depend>
  <depend>dc_lifecycle_manager</depend>
  <depend>dc_util</depend>
  <depend>diagnostic_msgs</depend>
  <!-- fastdds_statistics_backend (#392, fastdds_stats plugin) is deliberately not a <depend>: it
       has no rosdep/apt key on any distro and is only meaningful when the deployment runs Fast
       DDS as its RMW, so CMakeLists.txt finds it optionally and skips the plugin when it's
       absent instead of failing `rosdep install`/the whole workspace build for everyone else.
       See doc/src/dc/measurements/fastdds_stats.md. -->
  <depend>ffmpeg</depend>
  <depend>geometry_msgs</depend>
  <depend>lifecycle_msgs</depend>
  <depend>moveit_msgs</depend>
  <depend>nav2_msgs</depend>
  <depend>nav2_util</depend>
  <depend>nlohmann-json-dev</depend>
  <depend>nlohmann_json_schema_validator_vendor</depend>
  <depend>pkg-config</depend>
  <depend>pluginlib</depend>
  <depend>rclcpp_action</depend>
  <depend>sensor_msgs</depend>
  <depend>std_msgs</depend>
  <depend>tf2</depend>
  <depend>tf2_geometry_msgs</depend>
  <depend>tf2_ros</depend>
  <depend>unique_identifier_msgs</depend>
  <depend>yaml_cpp_vendor</depend>
  <depend>zxing-cpp</depend>

  <test_depend>ament_cmake_gtest</test_depend>
  <test_depend>socat</test_depend>

  <export>
    <build_type>ament_cmake</build_type>
    <dc_core plugin="${prefix}/condition_plugin.xml" />
    <dc_core plugin="${prefix}/measurement_plugin.xml" />
  </export>
</package>
